'use strict';

/**
 * user-study-schedule router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::user-study-schedule.user-study-schedule');
